import React from "react";

const RouteFallback = () => {
  return (
    <div className="min-h-screen bg-background flex items-center justify-center">
      {/* intentional empty fallback to avoid blank screen */}
    </div>
  );
};

export default RouteFallback;
